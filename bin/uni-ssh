#!/bin/bash

export HOST=stud18
while getopts "h:" opt; do
	case "$opt" in
		h)  export HOST=$OPTARG
			;;
	esac
done
shift $((OPTIND-1))

#ssh -CY -c "arcfour256,arcfour,blowfish-cbc" hilbsn@${HOST}.mathematik.uni-stuttgart.de

ssh -CY hilbsn@${HOST}.mathematik.uni-stuttgart.de
