#!/bin/bash


if [ -x "/usr/bin/chromium" ]; then

    # Chromium user flags
    export CHROMIUM_USER_FLAGS="--disk-cache-dir=$HOME/.cache --no-referrers"

    chromium

else

    xdg-open http://

fi

